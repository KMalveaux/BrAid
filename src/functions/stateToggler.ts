/**
 * This function is used to toggle the boolean state.
 * 
 * @param state A boolean value to be toggled.
 * @returns The opposite boolean value of the input state.
 */
function toggleState (state: boolean) {
    return !state;
};

export default toggleState;